apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  ports:
    - port: 3000
      targetPort: 3000
  selector:
    app: frontend
  image: krmp-d2hub-idock.9rum.cc/dev-test/repo_ef0b4fc9088f
  imagePullPolicy: Always